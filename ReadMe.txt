Its an python code which likes posts automatically by the hashtag you specify(at the bottom of the code)

To run this code you must need to install (selenium package) and also the (chrome webdriver)

Download chrome web driver from the given link:-https://chromedriver.chromium.org/downloads
and extract it to the files where your project_file is been located.
